package elte.move;

/**
 * The type Water move.
 */
public class WaterMove extends Move {
    /**
     * Instantiates a new Water move.
     * The type parameter of the Move comes from this class. We pass the enum value for WATER from MoveType.
     *
     * @param damage the damage
     * @param name   the name
     */
    public WaterMove(int damage, String name) {
        //EXERCISE
    }
}
